services:
  xrxs-263-sync:
    build: .
    container_name: xrxs-263-sync
    restart: unless-stopped
    env_file:
      - .env
    environment:
      TZ: Asia/Shanghai
    volumes:
      - ./data:/data
